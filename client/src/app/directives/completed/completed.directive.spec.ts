import { CompletedDirective } from './completed.directive';

describe('CompletedDirective', () => {
  it('should create an instance', () => {
    const directive = new CompletedDirective();
    expect(directive).toBeTruthy();
  });
});
