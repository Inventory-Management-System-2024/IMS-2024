import { ProcessingDirective } from './processing.directive';

describe('ProcessingDirective', () => {
  it('should create an instance', () => {
    const directive = new ProcessingDirective();
    expect(directive).toBeTruthy();
  });
});
