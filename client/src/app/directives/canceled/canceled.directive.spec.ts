import { CanceledDirective } from './canceled.directive';

describe('CanceledDirective', () => {
  it('should create an instance', () => {
    const directive = new CanceledDirective();
    expect(directive).toBeTruthy();
  });
});
